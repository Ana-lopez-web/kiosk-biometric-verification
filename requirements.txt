numpy>=1.24,<3.0
cryptography>=41.0
Pillow>=10.0

# Hardware drivers (physical NFC reader, etc.) live in requirements-hardware.txt.
# They are not needed to run the test suite, which uses mockable interfaces.

# --- testing ---
pytest>=7.4
pytest-cov>=4.1
freezegun>=1.4
